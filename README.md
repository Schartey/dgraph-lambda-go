# dgraph-lambda-go

```
#!/bin/sh

# Once lambda server is stable
# Build plugins
#echo "Start build plugins..."
#for f in ./plugins/*; do
#    echo "Building $f"
#    go build -buildmode=plugin -o ./$f ./$f
#done
#echo "Done building plugins"
```